node_modules/
build/
import/inbound/
.vitepress/cache/
.vitepress/dist/
.DS_Store

# Local secrets for the access gate. Never commit.
# Deliberately broader than .env and .env.*, which between them miss
# names like .env_production.
.env*
